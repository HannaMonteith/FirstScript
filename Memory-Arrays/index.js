console.log('hello');

const spellbook = ['Accio', 'Expecto Patronum', 'Wingardium Levisoa'];
let mannaPoints =50;

const castSpell = () => {
    // chose a random spell from the array
    const randomIndex = Math.floor(Math.random() * spellBook.length);
    const randomSpell = spellBook[randomIndex];
}